name: SimpleLock
version: 1.1.0
description: Container protection plugin for Spigot, Paper, and Purpur servers
main: me.fearlessstudios.simplelock.SimpleLockPlugin
api-version: '1.21'
author: FearlessStudios
softdepend: [Towny, Vault, GriefPrevention, Lands]

commands:
  simplelock:
    description: Main SimpleLock command
    usage: /simplelock <whitelist|transfer|info|adminbypass|forceunprotect|settings|reload|updateconfig>
    aliases: [slock]

permissions:
  simplelock.admin:
    description: Use admin override commands and toggle admin bypass mode
    default: op
  simplelock.settings:
    description: Open the SimpleLock settings GUI
    default: op
  simplelock.reload:
    description: Reload or update the SimpleLock config
    default: op
